name: "GitHub Action Wrapper for flyctl"
description: "Wraps the flyctl Command Line Tool"
author: JabobKrauskopf
branding:
  icon: "archive"
  color: "blue"
outputs:
  stdout:
    description: 'stdout of the flyctl command'
runs:
  using: "docker"
  image: "Dockerfile"
